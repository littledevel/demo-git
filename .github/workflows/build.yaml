name: Python app
on:
  push:
    branches: ["mybranch"]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: ls -l /srv2
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13' 